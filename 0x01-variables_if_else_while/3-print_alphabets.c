#include <stdio.h>

/**
 * main - Entry point
 * Description: Alphabet Game - CAPS & lowers
 * Return: 0
 */

int main(void)
{

	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(1);
	}
	for (i = 'A'; i <= 'Z'; i++)
	{
		putchar(1);
	}
	putchar('\n');

	return (0);
}
